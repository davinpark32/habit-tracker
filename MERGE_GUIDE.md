# Hopit 1차 프로토타입 개선 패치

기준 저장소: `davinpark32/habit-tracker`의 `main` 브랜치

## 포함 기능

- 목표 설정 FAB, 이름·시작일·기간·요일 반복 설정
- 운동/학습/창작/관계/도전/생활/업무 7개 타입 및 샘플 56개
- 샘플 선택과 키워드 기반 활동 타입 추천, 직접 드롭다운 지정
- 반복 목표 전체 일정 편집
- 오늘 화면과 날짜 화면에서 롱터치 완료 및 진행 피드백
- 완료 목표 탭으로 완료 취소
- 날짜별 `rewardedDates`로 사탕 최초 1회만 지급
- 부분 달성 캘린더 배경과 전체 달성 임시 `○` 스탬프
- 목표가 없는 날의 중립 처리
- 마지막 목표 완료 시 하루 클리어 연출
- 목표 완료 시 카테고리 사탕 보관함 추가
- 펫 화면 사탕 드래그/드롭 및 모바일 탭 급여
- 지혜·매력·활력·온기·의지·성실 성장 수치
- 사탕 5개 급여마다 내부 성장 단계 상승
- `localStorage` 저장 및 전체 데이터 초기화

## 데이터 규칙

- 목표 정의와 날짜별 완료 기록을 분리한다.
- 기간만 설정하면 시작일부터 종료일까지 매일 나타난다.
- 반복 요일을 고르면 기간 안에서 해당 요일에만 나타난다.
- 목표 완료를 취소해도 이미 획득한 사탕은 회수하지 않는다.
- 같은 목표 회차를 다시 완료해도 사탕은 중복 지급하지 않는다.
- 목표가 하나 이상 존재하고 모두 완료된 날만 하루 클리어다.
- 생활과 업무 사탕은 모두 `성실`을 올린다.

## 병합 방법

### patch 적용

저장소 루트에서:

```bash
git switch -c feature/hopit-prototype-v2
git apply --index hopit-prototype-v2.patch
npm install
npm run lint
npm run build
```

충돌이 있다면 `src/store.jsx`, `src/App.jsx`, `src/index.css` 순서로 현재 브랜치 변경과 비교한다. 이 패치는 데이터 모델과 화면 연결을 함께 바꾸므로 해당 파일 중 일부만 골라 적용하면 정상 동작하지 않는다.

### 전체 소스 사용

ZIP을 별도 폴더에 풀고 친구 작업 브랜치와 비교한 뒤 필요한 커밋을 만든다. ZIP에는 `.git`, `node_modules`, `dist`가 포함되지 않는다.

## 저장 위치와 초기화

브라우저 저장 키는 `hopit.prototype.v2`다. 같은 브라우저와 같은 사이트 주소에서 새로고침해도 유지된다. 펫 화면 우상단의 `전체 초기화`를 누르면 확인 후 목표, 완료 기록, 사탕, 성장 데이터가 모두 삭제된다.

## 검증 결과

- `oxlint src`: 오류 0개. 기존 구조 특성상 Fast Refresh 관련 경고 6개만 존재.
- `vite build`: 성공.
- `git diff --check`: 성공.
